<h2>Create a New List</h2>

<%= form_for [@user, @user.lists.build], html: {id: "create-list"} do |f| %>
  <%= f.text_field :name, placeholder: "list name" %>
  <%= f.submit "Create" %>
<% end %>

<% if @errors %>
  <% @errors.each do |error| %>
    <%= error %>
  <% end %>
<% end %>
